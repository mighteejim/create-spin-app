# Build and Run

Build:
`spin build`
`spin build --up`

Run:
`spin up --build`
`spin up --build --listen 127.0.0.1:3000`

Watch:
`spin watch`

App location:
Use `-f/--from` to point at a `spin.toml` or app directory.
