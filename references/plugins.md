# Plugins

Update cache:
`spin plugins update`

Search catalog:
`spin plugins search <filter>`

Install plugin:
`spin plugins install <name> --yes`

Show plugin info:
`spin plugins show <name>`

List installed:
`spin plugins list --installed`

Notes
- The curated plugin catalog is cached locally; run `spin plugins update` to see new versions.
